[profile.default]
src = "src"
out = "out"
libs = ["lib"]
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
]

[profile.fork]
# For fork tests, inherits default config
# Run with: SEPOLIA_RPC_URL=<your_rpc_url> forge test --match-contract GuardianForkTest -vvv
